<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ckh01.user.ex_1206">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntentMain2Activity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".IntentSubActivity" />
        <activity android:name=".HandlerActivity" />
        <activity android:name=".BackActivity"></activity>
        <activity android:name=".Back2Activity">
        </activity>
        <activity android:name=".BackButtonActivity">

        </activity>
    </application>

</manifest>